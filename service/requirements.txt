fastapi>=0.141.1
uvicorn>=0.52.4
cryptography>=50.0.1
# Postgres driver. The service runs on a free Render instance with no persistent
# disk, so the database is hosted rather than a local SQLite file.
psycopg[binary]>=3.3.4
httpx>=0.28.1
pydantic>=2.13.5
curl_cffi>=0.16.2
playwright>=1.62.0
playwright-stealth>=2.0.3
